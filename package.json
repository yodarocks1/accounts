{
  "name": "accounts-monorepo",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "Open-source, moddable double-entry accounting platform",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "eslint .",
    "typecheck": "pnpm -r typecheck",
    "demo": "node examples/demo/seed.mjs; node packages/cli/dist/main.js serve demo.sqlite --port 3000 --web packages/web/dist"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.0",
    "eslint": "^9.30.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.35.0",
    "vitest": "^3.2.4"
  }
}
